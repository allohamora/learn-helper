apiVersion: v1
kind: Service
metadata:
  name: learn-helper
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: learn-helper
  ports:
    - name: http
      port: 3000
      targetPort: http
